# Task 1.1: Initialize Backend Project - Design Document

**Date:** 2026-03-07
**Status:** Approved
**Phase:** 1 - Foundation

---

## Overview

Initialize the Bun.js backend project with Hono framework, full development tooling, and a scaffolding script for generating the Clean Architecture directory structure.

---

## Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Directory structure | Scaffold script | Reusable, clean initial state, on-demand generation |
| Development tools | Full (ESLint, Prettier, Husky) | Code quality from day one |
| Environment handling | dotenv | Explicit loading, widely supported |
| Test structure | Feature-based | Tests grouped by domain, easier navigation |

---

## Project Structure

### Initial Layout

```
backend/
├── scripts/
│   └── scaffold.ts              # Directory structure generator
├── src/
│   ├── main.ts                  # Entry point
│   ├── app.ts                   # Hono app configuration
│   └── infrastructure/
│       └── config/
│           └── index.ts         # Configuration loader
├── tests/
│   └── .gitkeep
├── package.json
├── tsconfig.json
├── eslint.config.js
├── .prettierrc
├── .husky/
│   ├── pre-commit
│   └── commit-msg
├── bunfig.toml
└── .gitignore
```

### Scaffolded Structure (via `bun run scripts/scaffold.ts`)

```
src/
├── presentation/
│   └── graphql/
│       ├── schema/
│       │   ├── types/
│       │   └── inputs/
│       └── resolvers/
├── application/
│   ├── services/
│   └── dto/
├── domain/
│   ├── entities/
│   ├── value-objects/
│   ├── errors/
│   └── interfaces/
├── infrastructure/
│   ├── database/
│   │   ├── models/
│   │   └── repositories/
│   ├── storage/
│   └── config/
└── utils/

tests/
├── unit/
│   ├── domain/
│   ├── services/
│   └── utils/
├── integration/
│   ├── repositories/
│   └── graphql/
└── e2e/
```

---

## Dependencies

### Production

| Package | Version | Purpose |
|---------|---------|---------|
| hono | ^4.0.0 | Web framework |
| graphql-yoga | ^5.0.0 | GraphQL server |
| mongoose | ^8.0.0 | MongoDB ODM |
| @pothos/core | ^3.0.0 | GraphQL schema builder |
| @pothos/plugin-zod | ^3.0.0 | Zod validation |
| zod | ^3.22.0 | Schema validation |
| jose | ^5.0.0 | JWT handling |
| minio | ^7.1.0 | File storage |
| dotenv | ^16.0.0 | Environment variables |
| graphql | ^16.8.0 | GraphQL core |

### Development

| Package | Version | Purpose |
|---------|---------|---------|
| @types/bun | latest | Type definitions |
| typescript | ^5.0.0 | TypeScript |
| eslint | ^8.55.0 | Linting |
| @typescript-eslint/eslint-plugin | ^6.0.0 | TS linting |
| @typescript-eslint/parser | ^6.0.0 | TS parser |
| prettier | ^3.0.0 | Formatting |
| eslint-config-prettier | ^9.0.0 | Prettier integration |
| husky | ^9.0.0 | Git hooks |
| lint-staged | ^15.0.0 | Staged linting |

---

## Configuration Files

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["bun-types"]
  },
  "include": ["src/**/*", "scripts/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}
```

### eslint.config.js (Flat Config)

```javascript
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';
import prettier from 'eslint-config-prettier';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
  prettier,
  {
    rules: {
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
      '@typescript-eslint/explicit-function-return-type': 'off',
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  }
);
```

### .prettierrc

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

### bunfig.toml

```toml
[test]
preload = ["./tests/setup.ts"]

[install]
production = false
```

### .gitignore

```
node_modules/
dist/
.env
.env.local
*.log
coverage/
```

---

## Core Files

### src/main.ts

```typescript
import { config } from 'dotenv';
config();

import { app } from './app';

const PORT = process.env.PORT || 4000;

console.log(`🚀 Server starting on port ${PORT}`);
console.log(`📊 Health check: http://localhost:${PORT}/health`);
console.log(`🔗 GraphQL: http://localhost:${PORT}/graphql`);

export default {
  port: PORT,
  fetch: app.fetch,
};
```

### src/app.ts

```typescript
import { Hono } from 'hono';
import { cors } from 'hono/cors';
import { logger } from 'hono/logger';
import { secureHeaders } from 'hono/secure-headers';

const app = new Hono();

app.use('*', secureHeaders());

app.use('*', cors({
  origin: ['http://localhost:3000', 'http://localhost:5173'],
  allowMethods: ['GET', 'POST', 'OPTIONS'],
  allowHeaders: ['Content-Type', 'Authorization'],
  credentials: true,
}));

if (process.env.NODE_ENV !== 'production') {
  app.use('*', logger());
}

app.get('/health', (c) => {
  return c.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    version: process.env.npm_package_version || '1.0.0',
  });
});

export { app };
```

### src/infrastructure/config/index.ts

```typescript
import 'dotenv/config';

export const config = {
  app: {
    nodeEnv: process.env.NODE_ENV || 'development',
    port: parseInt(process.env.PORT || '4000', 10),
  },
  database: {
    uri: process.env.MONGODB_URI || 'mongodb://localhost:27017/familytree',
  },
  jwt: {
    secret: process.env.JWT_SECRET || 'dev-secret-change-in-production',
    expiresIn: process.env.JWT_EXPIRES_IN || '7d',
  },
  minio: {
    endPoint: process.env.MINIO_ENDPOINT || 'localhost',
    port: parseInt(process.env.MINIO_PORT || '9000', 10),
    accessKey: process.env.MINIO_ACCESS_KEY || 'minioadmin',
    secretKey: process.env.MINIO_SECRET_KEY || 'minioadmin',
    bucket: process.env.MINIO_BUCKET || 'familytree-media',
    useSSL: process.env.MINIO_USE_SSL === 'true',
  },
} as const;
```

---

## Scaffold Script

### scripts/scaffold.ts

```typescript
import { mkdir, writeFile } from 'fs/promises';
import { join } from 'path';

const directories = [
  'src/presentation/graphql/schema/types',
  'src/presentation/graphql/schema/inputs',
  'src/presentation/graphql/resolvers',
  'src/application/services',
  'src/application/dto',
  'src/domain/entities',
  'src/domain/value-objects',
  'src/domain/errors',
  'src/domain/interfaces',
  'src/infrastructure/database/models',
  'src/infrastructure/database/repositories',
  'src/infrastructure/storage',
  'src/utils',
  'tests/unit/domain',
  'tests/unit/services',
  'tests/unit/utils',
  'tests/integration/repositories',
  'tests/integration/graphql',
  'tests/e2e',
  'tests/fixtures',
];

const indexFiles = [
  'src/domain/errors/index.ts',
  'src/domain/interfaces/index.ts',
  'src/infrastructure/database/models/index.ts',
  'src/infrastructure/database/repositories/index.ts',
  'src/application/services/index.ts',
  'src/application/dto/index.ts',
];

async function scaffold() {
  console.log('🏗️  Scaffolding project structure...\n');

  for (const dir of directories) {
    await mkdir(dir, { recursive: true });
    console.log(`✓ Created ${dir}`);
  }

  for (const file of indexFiles) {
    await writeFile(file, '// Export barrel\nexport {};\n');
    console.log(`✓ Created ${file}`);
  }

  await writeFile('tests/setup.ts', '// Test setup file\n');
  console.log('✓ Created tests/setup.ts');

  console.log('\n✅ Scaffold complete!');
}

scaffold().catch(console.error);
```

---

## Package.json Scripts

```json
{
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "build": "bun build src/main.ts --outdir dist",
    "start": "bun run dist/main.js",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "eslint src tests scripts",
    "lint:fix": "eslint src tests scripts --fix",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "scaffold": "bun run scripts/scaffold.ts",
    "prepare": "husky"
  }
}
```

---

## Husky Configuration

### .husky/pre-commit

```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged
```

### lint-staged (in package.json)

```json
{
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

---

## Success Criteria

- [ ] `bun run dev` starts server on port 4000
- [ ] `curl localhost:4000/health` returns `{"status":"ok",...}`
- [ ] `bun run lint` passes without errors
- [ ] `bun run scaffold` creates all directories
- [ ] Pre-commit hooks run on git commit
- [ ] TypeScript compiles without errors

---

## Atomic Steps

| # | Step | Command | Verification |
|---|------|---------|--------------|
| 1 | Create backend directory | `mkdir backend && cd backend` | `pwd` shows backend |
| 2 | Initialize package.json | `bun init -y` | package.json exists |
| 3 | Install production deps | `bun add hono graphql-yoga mongoose @pothos/core @pothos/plugin-zod zod jose minio dotenv graphql` | node_modules exists |
| 4 | Install dev deps | `bun add -d @types/bun typescript eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser prettier eslint-config-prettier husky lint-staged` | devDeps in package.json |
| 5 | Create tsconfig.json | Write file | `bun run build` succeeds |
| 6 | Create eslint.config.js | Write file | `bun run lint` works |
| 7 | Create .prettierrc | Write file | `bun run format` works |
| 8 | Create bunfig.toml | Write file | File exists |
| 9 | Create .gitignore | Write file | File exists |
| 10 | Setup Husky | `bunx husky init` | .husky exists |
| 11 | Add lint-staged config | Edit package.json | Config present |
| 12 | Create src/infrastructure/config | `mkdir -p src/infrastructure/config` | Directory exists |
| 13 | Create tests directory | `mkdir -p tests` | Directory exists |
| 14 | Create scripts directory | `mkdir scripts` | Directory exists |
| 15 | Create src/main.ts | Write file | File exists |
| 16 | Create src/app.ts | Write file | File exists |
| 17 | Create src/infrastructure/config/index.ts | Write file | File exists |
| 18 | Create scripts/scaffold.ts | Write file | File exists |
| 19 | Add package.json scripts | Edit package.json | Scripts present |
| 20 | Run scaffold | `bun run scaffold` | Directories created |
| 21 | Run server | `bun run dev` | Server on :4000 |
| 22 | Test health endpoint | `curl localhost:4000/health` | Returns JSON |
| 23 | Commit | `git add . && git commit -m "feat(backend): initialize project"` | Commit created |
