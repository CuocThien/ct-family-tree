# Task 1.1: Initialize Backend Project - Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Initialize a Bun.js backend project with Hono framework, full development tooling, and scaffolding script for Clean Architecture directory structure.

**Architecture:** Hybrid Clean Architecture with four layers (Presentation, Application, Domain, Infrastructure). Entry point loads environment variables, creates Hono app with security middleware, and exposes health check endpoint.

**Tech Stack:** Bun.js, Hono, TypeScript, ESLint (flat config), Prettier, Husky, lint-staged

---

## Task 1.1.1: Create Backend Directory and Initialize Package

**Files:**
- Create: `backend/package.json`

**Step 1: Create backend directory**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2
mkdir -p backend
```

Expected: Directory `backend/` created

**Step 2: Navigate to backend directory**

Run:
```bash
cd backend
```

Expected: Current directory is `backend/`

**Step 3: Initialize package.json with Bun**

Run:
```bash
bun init -y
```

Expected output:
```
Done! A package.json file was saved to the current directory.
 + package.json
```

**Step 4: Verify package.json exists**

Run:
```bash
cat package.json
```

Expected: JSON file with `name`, `version`, `module` fields

**Step 5: Commit**

```bash
git add backend/package.json
git commit -m "chore(backend): initialize package.json"
```

---

## Task 1.1.2: Install Production Dependencies

**Files:**
- Modify: `backend/package.json`
- Create: `backend/bun.lockb` (auto-generated)

**Step 1: Install core production dependencies**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun add hono graphql-yoga mongoose @pothos/core @pothos/plugin-zod zod jose minio dotenv graphql
```

Expected output:
```
 + hono@4.x.x
 + graphql-yoga@5.x.x
 + mongoose@8.x.x
 + @pothos/core@3.x.x
 + @pothos/plugin-zod@3.x.x
 + zod@3.x.x
 + jose@5.x.x
 + minio@7.x.x
 + dotenv@16.x.x
 + graphql@16.x.x
```

**Step 2: Verify dependencies installed**

Run:
```bash
cat package.json | grep -A 20 '"dependencies"'
```

Expected: All packages listed under `dependencies`

**Step 3: Verify node_modules exists**

Run:
```bash
ls -la node_modules | head -20
```

Expected: Directory with many package folders

**Step 4: Commit**

```bash
git add backend/package.json backend/bun.lockb
git commit -m "chore(backend): add production dependencies"
```

---

## Task 1.1.3: Install Development Dependencies

**Files:**
- Modify: `backend/package.json`

**Step 1: Install development dependencies**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun add -d @types/bun typescript eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser prettier eslint-config-prettier husky lint-staged typescript-eslint @eslint/js
```

Expected output:
```
 + @types/bun@latest
 + typescript@5.x.x
 + eslint@8.x.x
 + @typescript-eslint/eslint-plugin@6.x.x
 + @typescript-eslint/parser@6.x.x
 + prettier@3.x.x
 + eslint-config-prettier@9.x.x
 + husky@9.x.x
 + lint-staged@15.x.x
 + typescript-eslint@7.x.x
 + @eslint/js@9.x.x
```

**Step 2: Verify devDependencies**

Run:
```bash
cat package.json | grep -A 15 '"devDependencies"'
```

Expected: All packages listed under `devDependencies`

**Step 3: Commit**

```bash
git add backend/package.json backend/bun.lockb
git commit -m "chore(backend): add development dependencies"
```

---

## Task 1.1.4: Create TypeScript Configuration

**Files:**
- Create: `backend/tsconfig.json`

**Step 1: Create tsconfig.json**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["bun-types"]
  },
  "include": ["src/**/*", "scripts/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}
EOF
```

Expected: File `tsconfig.json` created

**Step 2: Verify TypeScript compiles (empty project should pass)**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bunx tsc --noEmit
```

Expected: No errors (may warn about no files)

**Step 3: Commit**

```bash
git add backend/tsconfig.json
git commit -m "chore(backend): add TypeScript configuration"
```

---

## Task 1.1.5: Create ESLint Configuration

**Files:**
- Create: `backend/eslint.config.js`

**Step 1: Create eslint.config.js (flat config)**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/eslint.config.js << 'EOF'
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';
import prettier from 'eslint-config-prettier';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
  prettier,
  {
    rules: {
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
      '@typescript-eslint/explicit-function-return-type': 'off',
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  }
);
EOF
```

Expected: File `eslint.config.js` created

**Step 2: Commit**

```bash
git add backend/eslint.config.js
git commit -m "chore(backend): add ESLint flat config"
```

---

## Task 1.1.6: Create Prettier Configuration

**Files:**
- Create: `backend/.prettierrc`

**Step 1: Create .prettierrc**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/.prettierrc << 'EOF'
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
EOF
```

Expected: File `.prettierrc` created

**Step 2: Commit**

```bash
git add backend/.prettierrc
git commit -m "chore(backend): add Prettier configuration"
```

---

## Task 1.1.7: Create Bun Configuration

**Files:**
- Create: `backend/bunfig.toml`

**Step 1: Create bunfig.toml**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/bunfig.toml << 'EOF'
[test]
preload = ["./tests/setup.ts"]

[install]
production = false
EOF
```

Expected: File `bunfig.toml` created

**Step 2: Commit**

```bash
git add backend/bunfig.toml
git commit -m "chore(backend): add Bun configuration"
```

---

## Task 1.1.8: Create .gitignore

**Files:**
- Create: `backend/.gitignore`

**Step 1: Create .gitignore**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/.gitignore << 'EOF'
# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
bun-debug.log*

# Coverage
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test
.jest-results/
EOF
```

Expected: File `.gitignore` created

**Step 2: Commit**

```bash
git add backend/.gitignore
git commit -m "chore(backend): add .gitignore"
```

---

## Task 1.1.9: Setup Husky for Git Hooks

**Files:**
- Create: `backend/.husky/_/husky.sh`
- Create: `backend/.husky/pre-commit`

**Step 1: Initialize Husky**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bunx husky init
```

Expected output:
```
.git/hooks/pre-commit created
.husky/pre-commit created
```

**Step 2: Verify .husky directory**

Run:
```bash
ls -la .husky/
```

Expected: Directory with `pre-commit` file

**Step 3: Commit**

```bash
git add backend/.husky/
git commit -m "chore(backend): setup Husky git hooks"
```

---

## Task 1.1.10: Add lint-staged Configuration and Scripts

**Files:**
- Modify: `backend/package.json`

**Step 1: Read current package.json**

Run:
```bash
cat /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/package.json
```

**Step 2: Update package.json with scripts and lint-staged**

Create the updated package.json:

```json
{
  "name": "familytree-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "build": "bun build src/main.ts --outdir dist",
    "start": "bun run dist/main.js",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "eslint src tests scripts",
    "lint:fix": "eslint src tests scripts --fix",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\" \"scripts/**/*.ts\"",
    "scaffold": "bun run scripts/scaffold.ts",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
```

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/package.json << 'EOF'
{
  "name": "familytree-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "build": "bun build src/main.ts --outdir dist",
    "start": "bun run dist/main.js",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "eslint src tests scripts",
    "lint:fix": "eslint src tests scripts --fix",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\" \"scripts/**/*.ts\"",
    "scaffold": "bun run scripts/scaffold.ts",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
EOF
```

Note: This will remove dependencies section temporarily - we'll restore them in next step.

**Step 3: Re-install dependencies to restore them in package.json**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun install
```

Expected: Dependencies restored in package.json

**Step 4: Verify scripts exist**

Run:
```bash
cat package.json | grep -A 15 '"scripts"'
```

Expected: All scripts present

**Step 5: Update .husky/pre-commit**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/.husky/pre-commit << 'EOF'
npx lint-staged
EOF
```

**Step 6: Commit**

```bash
git add backend/package.json backend/.husky/pre-commit
git commit -m "chore(backend): add npm scripts and lint-staged config"
```

---

## Task 1.1.11: Create Directory Structure

**Files:**
- Create: `backend/src/` directory
- Create: `backend/src/infrastructure/config/` directory
- Create: `backend/tests/` directory
- Create: `backend/scripts/` directory

**Step 1: Create src directory structure**

Run:
```bash
mkdir -p /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/infrastructure/config
```

**Step 2: Create tests directory**

Run:
```bash
mkdir -p /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tests
```

**Step 3: Create scripts directory**

Run:
```bash
mkdir -p /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/scripts
```

**Step 4: Add .gitkeep to tests**

Run:
```bash
touch /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tests/.gitkeep
```

**Step 5: Verify structure**

Run:
```bash
ls -la /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/
ls -la /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tests/
```

Expected: Directories exist

**Step 6: Commit**

```bash
git add backend/src/ backend/tests/.gitkeep backend/scripts/
git commit -m "chore(backend): create initial directory structure"
```

---

## Task 1.1.12: Create Hono App Configuration

**Files:**
- Create: `backend/src/app.ts`

**Step 1: Create src/app.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/app.ts << 'EOF'
import { Hono } from 'hono';
import { cors } from 'hono/cors';
import { logger } from 'hono/logger';
import { secureHeaders } from 'hono/secure-headers';

const app = new Hono();

// Security headers
app.use('*', secureHeaders());

// CORS configuration
app.use(
  '*',
  cors({
    origin: ['http://localhost:3000', 'http://localhost:5173'],
    allowMethods: ['GET', 'POST', 'OPTIONS'],
    allowHeaders: ['Content-Type', 'Authorization'],
    credentials: true,
  })
);

// Request logging (development only)
if (process.env.NODE_ENV !== 'production') {
  app.use('*', logger());
}

// Health check endpoint
app.get('/health', (c) => {
  return c.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    version: process.env.npm_package_version || '1.0.0',
  });
});

// GraphQL endpoint will be added here

export { app };
EOF
```

Expected: File `src/app.ts` created

**Step 2: Verify file exists**

Run:
```bash
cat /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/app.ts
```

Expected: File contents displayed

**Step 3: Commit**

```bash
git add backend/src/app.ts
git commit -m "feat(backend): add Hono app with CORS and security headers"
```

---

## Task 1.1.13: Create Entry Point (main.ts)

**Files:**
- Create: `backend/src/main.ts`

**Step 1: Create src/main.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/main.ts << 'EOF'
import { config } from 'dotenv';
config();

import { app } from './app';

const PORT = process.env.PORT || 4000;

console.log(`🚀 Server starting on port ${PORT}`);
console.log(`📊 Health check: http://localhost:${PORT}/health`);
console.log(`🔗 GraphQL: http://localhost:${PORT}/graphql`);

export default {
  port: PORT,
  fetch: app.fetch,
};
EOF
```

Expected: File `src/main.ts` created

**Step 2: Verify file exists**

Run:
```bash
cat /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/main.ts
```

Expected: File contents displayed

**Step 3: Commit**

```bash
git add backend/src/main.ts
git commit -m "feat(backend): add entry point with dotenv loading"
```

---

## Task 1.1.14: Create Configuration Module

**Files:**
- Create: `backend/src/infrastructure/config/index.ts`

**Step 1: Create config/index.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/infrastructure/config/index.ts << 'EOF'
import 'dotenv/config';

export const config = {
  app: {
    nodeEnv: process.env.NODE_ENV || 'development',
    port: parseInt(process.env.PORT || '4000', 10),
  },
  database: {
    uri: process.env.MONGODB_URI || 'mongodb://localhost:27017/familytree',
  },
  jwt: {
    secret: process.env.JWT_SECRET || 'dev-secret-change-in-production',
    expiresIn: process.env.JWT_EXPIRES_IN || '7d',
  },
  minio: {
    endPoint: process.env.MINIO_ENDPOINT || 'localhost',
    port: parseInt(process.env.MINIO_PORT || '9000', 10),
    accessKey: process.env.MINIO_ACCESS_KEY || 'minioadmin',
    secretKey: process.env.MINIO_SECRET_KEY || 'minioadmin',
    bucket: process.env.MINIO_BUCKET || 'familytree-media',
    useSSL: process.env.MINIO_USE_SSL === 'true',
  },
} as const;
EOF
```

Expected: File `src/infrastructure/config/index.ts` created

**Step 2: Verify file exists**

Run:
```bash
cat /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/infrastructure/config/index.ts
```

Expected: File contents displayed

**Step 3: Commit**

```bash
git add backend/src/infrastructure/config/index.ts
git commit -m "feat(backend): add configuration module"
```

---

## Task 1.1.15: Create Scaffold Script

**Files:**
- Create: `backend/scripts/scaffold.ts`

**Step 1: Create scaffold.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/scripts/scaffold.ts << 'EOF'
import { mkdir, writeFile } from 'fs/promises';

const directories = [
  'src/presentation/graphql/schema/types',
  'src/presentation/graphql/schema/inputs',
  'src/presentation/graphql/resolvers',
  'src/application/services',
  'src/application/dto',
  'src/domain/entities',
  'src/domain/value-objects',
  'src/domain/errors',
  'src/domain/interfaces',
  'src/infrastructure/database/models',
  'src/infrastructure/database/repositories',
  'src/infrastructure/storage',
  'src/utils',
  'tests/unit/domain',
  'tests/unit/services',
  'tests/unit/utils',
  'tests/integration/repositories',
  'tests/integration/graphql',
  'tests/e2e',
  'tests/fixtures',
];

const indexFiles = [
  'src/domain/errors/index.ts',
  'src/domain/interfaces/index.ts',
  'src/infrastructure/database/models/index.ts',
  'src/infrastructure/database/repositories/index.ts',
  'src/application/services/index.ts',
  'src/application/dto/index.ts',
];

async function scaffold() {
  console.log('🏗️  Scaffolding project structure...\n');

  // Create directories
  for (const dir of directories) {
    await mkdir(dir, { recursive: true });
    console.log(`✓ Created ${dir}`);
  }

  // Create index files
  for (const file of indexFiles) {
    await writeFile(file, '// Export barrel\nexport {};\n');
    console.log(`✓ Created ${file}`);
  }

  // Create test setup file
  await writeFile('tests/setup.ts', '// Test setup file\n');
  console.log('✓ Created tests/setup.ts');

  console.log('\n✅ Scaffold complete!');
}

scaffold().catch(console.error);
EOF
```

Expected: File `scripts/scaffold.ts` created

**Step 2: Verify file exists**

Run:
```bash
cat /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/scripts/scaffold.ts
```

Expected: File contents displayed

**Step 3: Commit**

```bash
git add backend/scripts/scaffold.ts
git commit -m "feat(backend): add scaffold script for Clean Architecture structure"
```

---

## Task 1.1.16: Run Scaffold Script

**Files:**
- Multiple directories created by scaffold

**Step 1: Run scaffold script**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun run scaffold
```

Expected output:
```
🏗️  Scaffolding project structure...

✓ Created src/presentation/graphql/schema/types
✓ Created src/presentation/graphql/schema/inputs
✓ Created src/presentation/graphql/resolvers
✓ Created src/application/services
✓ Created src/application/dto
✓ Created src/domain/entities
✓ Created src/domain/value-objects
✓ Created src/domain/errors
✓ Created src/domain/interfaces
✓ Created src/infrastructure/database/models
✓ Created src/infrastructure/database/repositories
✓ Created src/infrastructure/storage
✓ Created src/utils
✓ Created tests/unit/domain
✓ Created tests/unit/services
✓ Created tests/unit/utils
✓ Created tests/integration/repositories
✓ Created tests/integration/graphql
✓ Created tests/e2e
✓ Created tests/fixtures
✓ Created src/domain/errors/index.ts
✓ Created src/domain/interfaces/index.ts
✓ Created src/infrastructure/database/models/index.ts
✓ Created src/infrastructure/database/repositories/index.ts
✓ Created src/application/services/index.ts
✓ Created src/application/dto/index.ts
✓ Created tests/setup.ts

✅ Scaffold complete!
```

**Step 2: Verify directory structure**

Run:
```bash
ls -la /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/
ls -la /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tests/
```

Expected: All directories present

**Step 3: Commit scaffolded structure**

```bash
git add backend/
git commit -m "chore(backend): scaffold Clean Architecture directory structure"
```

---

## Task 1.1.17: Verify TypeScript Compilation

**Files:**
- None (verification only)

**Step 1: Run TypeScript compiler**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bunx tsc --noEmit
```

Expected: No errors

**Step 2: If errors exist, fix them**

Common fixes:
- Add `export {}` to files that don't have exports
- Check import paths

---

## Task 1.1.18: Verify Linting Works

**Files:**
- None (verification only)

**Step 1: Run ESLint**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun run lint
```

Expected: No errors or warnings about code style

**Step 2: If errors, run lint:fix**

Run:
```bash
bun run lint:fix
```

---

## Task 1.1.19: Start Development Server

**Files:**
- None (verification only)

**Step 1: Start server**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun run dev
```

Expected output:
```
🚀 Server starting on port 4000
📊 Health check: http://localhost:4000/health
🔗 GraphQL: http://localhost:4000/graphql
```

**Step 2: Verify server is running**

Open new terminal, run:
```bash
curl http://localhost:4000/health
```

Expected output:
```json
{"status":"ok","timestamp":"2026-03-07T...","version":"1.0.0"}
```

**Step 3: Stop server**

Press `Ctrl+C` in the terminal running the server

---

## Task 1.1.20: Final Verification and Commit

**Files:**
- None (verification only)

**Step 1: Verify all files exist**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
ls -la
ls -la src/
ls -la tests/
```

Expected: All expected files and directories present

**Step 2: Verify package.json scripts**

Run:
```bash
cat package.json | jq '.scripts'
```

Expected:
```json
{
  "dev": "bun run --watch src/main.ts",
  "build": "bun build src/main.ts --outdir dist",
  "start": "bun run dist/main.js",
  "test": "bun test",
  "test:coverage": "bun test --coverage",
  "lint": "eslint src tests scripts",
  "lint:fix": "eslint src tests scripts --fix",
  "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\" \"scripts/**/*.ts\"",
  "scaffold": "bun run scripts/scaffold.ts",
  "prepare": "husky"
}
```

**Step 3: Create .env file from example**

Run:
```bash
cp ../.env.example .env
```

**Step 4: Final commit**

```bash
git add backend/
git commit -m "feat(backend): complete backend project initialization

- Initialize Bun.js project with Hono framework
- Add TypeScript, ESLint, Prettier configuration
- Setup Husky git hooks with lint-staged
- Create Clean Architecture directory structure via scaffold script
- Add health check endpoint
- Add configuration module for environment variables"
```

---

## Success Criteria Checklist

After completing all tasks, verify:

- [ ] `bun run dev` starts server on port 4000
- [ ] `curl localhost:4000/health` returns `{"status":"ok",...}`
- [ ] `bun run lint` passes without errors
- [ ] `bun run scaffold` creates all directories
- [ ] TypeScript compiles without errors (`bunx tsc --noEmit`)
- [ ] All configuration files exist (tsconfig.json, eslint.config.js, .prettierrc)
- [ ] Git hooks work (test with a commit)

---

## Execution Handoff

Plan complete and saved to `docs/plans/2026-03-07-task-1.1-implementation.md`.

**Two execution options:**

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

**Which approach?**
