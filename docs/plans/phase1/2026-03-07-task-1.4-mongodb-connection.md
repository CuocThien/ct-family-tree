# Task 1.4: Setup MongoDB Connection - Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Create MongoDB connection module with connection management and error handling.

**Files:**
- Create: `backend/src/infrastructure/database/connection.ts`
- Test: `backend/tests/unit/infrastructure/database/connection.test.ts`

---

## Step 1: Create tests directory structure

Run:
```bash
mkdir -p /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tests/unit/infrastructure/database
```

---

## Step 2: Write failing test

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/tests/unit/infrastructure/database/connection.test.ts << 'EOF'
import { describe, test, expect, beforeAll, afterAll } from 'bun:test';
import { connectToDatabase, disconnectFromDatabase, getDatabaseConnectionStatus } from '@/infrastructure/database/connection';

describe('Database Connection', () => {
  afterAll(async () => {
    await disconnectFromDatabase();
  });

  test('should connect to MongoDB', async () => {
    await connectToDatabase();
    const status = getDatabaseConnectionStatus();
    expect(status).toBe(true);
  });

  test('should disconnect from MongoDB', async () => {
    await disconnectFromDatabase();
    const status = getDatabaseConnectionStatus();
    expect(status).toBe(false);
  });

  test('should handle multiple connect calls gracefully', async () => {
    await connectToDatabase();
    await connectToDatabase(); // Should not throw
    expect(getDatabaseConnectionStatus()).toBe(true);
  });
});
EOF
```

---

## Step 3: Run test to verify failure

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun test tests/unit/infrastructure/database/connection.test.ts
```

Expected: FAIL - Cannot find module

---

## Step 4: Create connection.ts

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend/src/infrastructure/database/connection.ts << 'EOF'
import mongoose from 'mongoose';
import { config } from '@/infrastructure/config';

let isConnected = false;

export async function connectToDatabase(): Promise<void> {
  if (isConnected) {
    return;
  }

  try {
    await mongoose.connect(config.database.uri);
    isConnected = true;
    console.log('📦 Connected to MongoDB');
  } catch (error) {
    console.error('❌ MongoDB connection error:', error);
    throw error;
  }
}

export async function disconnectFromDatabase(): Promise<void> {
  if (!isConnected) {
    return;
  }

  try {
    await mongoose.disconnect();
    isConnected = false;
    console.log('📦 Disconnected from MongoDB');
  } catch (error) {
    console.error('❌ MongoDB disconnection error:', error);
    throw error;
  }
}

export function getDatabaseConnectionStatus(): boolean {
  return isConnected;
}

export function getMongooseInstance(): typeof mongoose {
  return mongoose;
}
EOF
```

---

## Step 5: Run test to verify pass

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/backend
bun test tests/unit/infrastructure/database/connection.test.ts
```

Expected: PASS

---

## Step 6: Commit

Run:
```bash
git add backend/src/infrastructure/database/connection.ts backend/tests/
git commit -m "feat(backend): add MongoDB connection management"
```

---

## Success Criteria

- [ ] `connectToDatabase()` establishes connection
- [ ] `disconnectFromDatabase()` closes connection
- [ ] `getDatabaseConnectionStatus()` returns correct status
- [ ] Multiple connect calls don't throw errors
- [ ] Tests pass
