# Task 1.2: Initialize Frontend Project - Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Initialize a React + Vite + TypeScript frontend project with Ant Design, Tailwind CSS, Jotai, Apollo Client, and React Router.

**Architecture:** Feature-based organization with providers wrapping the app. GraphQL client configured with auth and error handling. i18n setup with EN/VI support.

**Tech Stack:** React 18, Vite, TypeScript, Ant Design, Tailwind CSS, Jotai, Apollo Client, React Router, react-i18next, Vitest

---

## Task 1.2.1: Create Frontend Directory and Initialize Vite

**Files:**
- Create: `frontend/` directory
- Create: `frontend/package.json`

**Step 1: Create frontend directory**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2
mkdir -p frontend
```

Expected: Directory `frontend/` created

**Step 2: Navigate to frontend directory**

Run:
```bash
cd frontend
```

**Step 3: Initialize Vite project with React TypeScript template**

Run:
```bash
npm create vite@latest . -- --template react-ts
```

Expected output:
```
✔ Package name: frontend
✔ Select a framework: › React
✔ Select a variant: › TypeScript
Scaffolding project in /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend...
Done. Now run:
  npm install
```

**Step 4: Verify files created**

Run:
```bash
ls -la
```

Expected: Files including `package.json`, `tsconfig.json`, `vite.config.ts`, `src/`

**Step 5: Commit**

```bash
git add frontend/
git commit -m "chore(frontend): initialize Vite React TypeScript project"
```

---

## Task 1.2.2: Install Production Dependencies

**Files:**
- Modify: `frontend/package.json`

**Step 1: Install production dependencies**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm install react-router-dom @apollo/client graphql jotai antd react-hook-form @hookform/resolvers zod react-i18next i18next i18next-browser-languagedetector date-fns
```

Expected output:
```
added XX packages in Xs
```

**Step 2: Verify dependencies**

Run:
```bash
cat package.json | grep -A 20 '"dependencies"'
```

Expected: All packages listed

**Step 3: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "chore(frontend): add production dependencies"
```

---

## Task 1.2.3: Install Development Dependencies

**Files:**
- Modify: `frontend/package.json`

**Step 1: Install Tailwind and PostCSS**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm install -D tailwindcss postcss autoprefixer
```

**Step 2: Install testing dependencies**

Run:
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitest/ui
```

**Step 3: Install ESLint and Prettier**

Run:
```bash
npm install -D eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-react-refresh prettier eslint-config-prettier
```

**Step 4: Verify devDependencies**

Run:
```bash
cat package.json | grep -A 25 '"devDependencies"'
```

Expected: All dev packages listed

**Step 5: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "chore(frontend): add development dependencies"
```

---

## Task 1.2.4: Create TypeScript Configuration

**Files:**
- Create: `frontend/tsconfig.json`
- Create: `frontend/tsconfig.node.json`

**Step 1: Update tsconfig.json**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
EOF
```

**Step 2: Create tsconfig.node.json**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/tsconfig.node.json << 'EOF'
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts", "vitest.config.ts"]
}
EOF
```

**Step 3: Verify TypeScript compiles**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm run build
```

Expected: Build succeeds (may have warnings)

**Step 4: Commit**

```bash
git add frontend/tsconfig.json frontend/tsconfig.node.json
git commit -m "chore(frontend): configure TypeScript with path aliases"
```

---

## Task 1.2.5: Create Vite Configuration

**Files:**
- Create: `frontend/vite.config.ts`

**Step 1: Update vite.config.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/vite.config.ts << 'EOF'
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 3000,
    proxy: {
      '/graphql': {
        target: 'http://localhost:4000',
        changeOrigin: true,
      },
    },
  },
});
EOF
```

**Step 2: Verify Vite runs**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm run dev &
sleep 3
curl http://localhost:3000 | head -20
kill %1
```

Expected: HTML returned

**Step 3: Commit**

```bash
git add frontend/vite.config.ts
git commit -m "chore(frontend): configure Vite with path aliases and GraphQL proxy"
```

---

## Task 1.2.6: Create Tailwind Configuration

**Files:**
- Create: `frontend/tailwind.config.js`
- Create: `frontend/postcss.config.js`

**Step 1: Create tailwind.config.js**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/tailwind.config.js << 'EOF'
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  // Disable preflight to avoid conflicts with Ant Design
  corePlugins: {
    preflight: false,
  },
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
        },
      },
    },
  },
  plugins: [],
};
EOF
```

**Step 2: Create postcss.config.js**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/postcss.config.js << 'EOF'
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
EOF
```

**Step 3: Commit**

```bash
git add frontend/tailwind.config.js frontend/postcss.config.js
git commit -m "chore(frontend): configure Tailwind CSS with Ant Design compatibility"
```

---

## Task 1.2.7: Create Vitest Configuration

**Files:**
- Create: `frontend/vitest.config.ts`
- Create: `frontend/tests/setup.ts`

**Step 1: Create vitest.config.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/vitest.config.ts << 'EOF'
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    globals: true,
    include: ['tests/**/*.{test,spec}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      exclude: ['node_modules/', 'tests/', 'src/vite-env.d.ts'],
    },
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
EOF
```

**Step 2: Create tests directory and setup file**

Run:
```bash
mkdir -p /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/tests
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/tests/setup.ts << 'EOF'
import '@testing-library/jest-dom';
EOF
```

**Step 3: Commit**

```bash
git add frontend/vitest.config.ts frontend/tests/
git commit -m "chore(frontend): configure Vitest for unit testing"
```

---

## Task 1.2.8: Create ESLint and Prettier Configuration

**Files:**
- Create: `frontend/.eslintrc.cjs`
- Create: `frontend/.prettierrc`

**Step 1: Create .eslintrc.cjs**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/.eslintrc.cjs << 'EOF'
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react/jsx-runtime',
    'plugin:react-hooks/recommended',
    'prettier',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
    'react/prop-types': 'off',
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
  settings: {
    react: {
      version: 'detect',
    },
  },
};
EOF
```

**Step 2: Create .prettierrc**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/.prettierrc << 'EOF'
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
EOF
```

**Step 3: Commit**

```bash
git add frontend/.eslintrc.cjs frontend/.prettierrc
git commit -m "chore(frontend): configure ESLint and Prettier"
```

---

## Task 1.2.9: Create Directory Structure

**Files:**
- Create directories: `src/app/providers/`, `src/features/`, `src/shared/`, `src/graphql/`, `src/i18n/`, `src/styles/`
- Create directories: `public/locales/en/`, `public/locales/vi/`

**Step 1: Create all directories**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
mkdir -p src/app/providers
mkdir -p src/features
mkdir -p src/shared/components/layout
mkdir -p src/shared/components/ui
mkdir -p src/shared/hooks
mkdir -p src/shared/utils
mkdir -p src/graphql
mkdir -p src/i18n
mkdir -p src/styles
mkdir -p public/locales/en
mkdir -p public/locales/vi
```

**Step 2: Add .gitkeep files to empty directories**

Run:
```bash
touch src/features/.gitkeep
touch src/shared/components/ui/.gitkeep
touch src/shared/hooks/.gitkeep
touch src/shared/utils/.gitkeep
```

**Step 3: Commit**

```bash
git add frontend/src/ frontend/public/locales/
git commit -m "chore(frontend): create directory structure"
```

---

## Task 1.2.10: Create Global Styles

**Files:**
- Create: `frontend/src/styles/global.css`

**Step 1: Create global.css**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/styles/global.css << 'EOF'
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Ant Design overrides */
.ant-layout {
  min-height: 100vh;
}

.ant-layout-sider {
  background: #001529;
}

/* Custom scrollbar */
.scrollbar-thin::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

.scrollbar-thin::-webkit-scrollbar-track {
  background: transparent;
}

.scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: rgba(0, 0, 0, 0.2);
  border-radius: 3px;
}

.scrollbar-thin::-webkit-scrollbar-thumb:hover {
  background-color: rgba(0, 0, 0, 0.3);
}

/* Custom utilities */
@layer utilities {
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
EOF
```

**Step 2: Delete default App.css**

Run:
```bash
rm /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/App.css
rm /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/index.css
```

**Step 3: Commit**

```bash
git add frontend/src/styles/ frontend/src/App.css frontend/src/index.css
git commit -m "feat(frontend): add global styles with Tailwind"
```

---

## Task 1.2.11: Create i18n Configuration

**Files:**
- Create: `frontend/src/i18n/index.ts`
- Create: `frontend/public/locales/en/translation.json`
- Create: `frontend/public/locales/vi/translation.json`

**Step 1: Create i18n/index.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/i18n/index.ts << 'EOF'
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

// Import translations
import enTranslation from '../../public/locales/en/translation.json';
import viTranslation from '../../public/locales/vi/translation.json';

i18n.use(LanguageDetector).use(initReactI18next).init({
  fallbackLng: 'en',
  supportedLngs: ['en', 'vi'],
  interpolation: {
    escapeValue: false,
  },
  detection: {
    order: ['localStorage', 'navigator'],
    caches: ['localStorage'],
  },
  resources: {
    en: {
      translation: enTranslation,
    },
    vi: {
      translation: viTranslation,
    },
  },
});

export default i18n;
EOF
```

**Step 2: Create English translations**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/public/locales/en/translation.json << 'EOF'
{
  "welcome": "Welcome to Family Tree",
  "dashboard": "Dashboard",
  "members": "Members",
  "tree": "Family Tree",
  "settings": "Settings",
  "login": "Login",
  "logout": "Logout",
  "register": "Register",
  "loading": "Loading...",
  "error": "Error",
  "success": "Success",
  "cancel": "Cancel",
  "save": "Save",
  "delete": "Delete",
  "edit": "Edit",
  "add": "Add",
  "search": "Search",
  "noResults": "No results found",
  "confirmDelete": "Are you sure you want to delete this?"
}
EOF
```

**Step 3: Create Vietnamese translations**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/public/locales/vi/translation.json << 'EOF'
{
  "welcome": "Chào mừng đến với Gia Phả",
  "dashboard": "Bảng điều khiển",
  "members": "Thành viên",
  "tree": "Cây gia phả",
  "settings": "Cài đặt",
  "login": "Đăng nhập",
  "logout": "Đăng xuất",
  "register": "Đăng ký",
  "loading": "Đang tải...",
  "error": "Lỗi",
  "success": "Thành công",
  "cancel": "Hủy",
  "save": "Lưu",
  "delete": "Xóa",
  "edit": "Sửa",
  "add": "Thêm",
  "search": "Tìm kiếm",
  "noResults": "Không tìm thấy kết quả",
  "confirmDelete": "Bạn có chắc chắn muốn xóa không?"
}
EOF
```

**Step 4: Commit**

```bash
git add frontend/src/i18n/ frontend/public/locales/
git commit -m "feat(frontend): add i18n configuration with EN/VI translations"
```

---

## Task 1.2.12: Create GraphQL Client

**Files:**
- Create: `frontend/src/graphql/client.ts`

**Step 1: Create graphql/client.ts**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/graphql/client.ts << 'EOF'
import { ApolloClient, InMemoryCache, HttpLink, from } from '@apollo/client';
import { setContext } from '@apollo/client/link/context';
import { onError } from '@apollo/client/link/error';

const httpLink = new HttpLink({
  uri: '/graphql',
  credentials: 'include',
});

const authLink = setContext((_, { headers }) => {
  const token = localStorage.getItem('auth_token');
  return {
    headers: {
      ...headers,
      authorization: token ? `Bearer ${token}` : '',
    },
  };
});

const errorLink = onError(({ graphQLErrors, networkError }) => {
  if (graphQLErrors) {
    graphQLErrors.forEach(({ message, locations, path }) => {
      console.error(`[GraphQL error]: Message: ${message}, Location: ${locations}, Path: ${path}`);
    });
  }
  if (networkError) {
    console.error(`[Network error]: ${networkError}`);
  }
});

export const apolloClient = new ApolloClient({
  link: from([errorLink, authLink, httpLink]),
  cache: new InMemoryCache({
    typePolicies: {
      Query: {
        fields: {
          members: {
            merge: false,
          },
        },
      },
    },
  }),
  defaultOptions: {
    watchQuery: {
      fetchPolicy: 'cache-and-network',
    },
  },
});
EOF
```

**Step 2: Commit**

```bash
git add frontend/src/graphql/
git commit -m "feat(frontend): add Apollo Client configuration"
```

---

## Task 1.2.13: Create Providers

**Files:**
- Create: `frontend/src/app/providers/ApolloProvider.tsx`
- Create: `frontend/src/app/providers/I18nProvider.tsx`

**Step 1: Create ApolloProvider.tsx**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/app/providers/ApolloProvider.tsx << 'EOF'
import { ApolloProvider as BaseApolloProvider } from '@apollo/client';
import { ReactNode } from 'react';
import { apolloClient } from '@/graphql/client';

interface ApolloProviderWrapperProps {
  children: ReactNode;
}

export function ApolloProviderWrapper({ children }: ApolloProviderWrapperProps) {
  return <BaseApolloProvider client={apolloClient}>{children}</BaseApolloProvider>;
}
EOF
```

**Step 2: Create I18nProvider.tsx**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/app/providers/I18nProvider.tsx << 'EOF'
import { ReactNode, Suspense } from 'react';
import { I18nextProvider } from 'react-i18next';
import { Spin } from 'antd';
import i18n from '@/i18n';

interface I18nProviderProps {
  children: ReactNode;
}

export function I18nProvider({ children }: I18nProviderProps) {
  return (
    <Suspense
      fallback={
        <div className="min-h-screen flex items-center justify-center">
          <Spin size="large" />
        </div>
      }
    >
      <I18nextProvider i18n={i18n}>{children}</I18nextProvider>
    </Suspense>
  );
}
EOF
```

**Step 3: Commit**

```bash
git add frontend/src/app/providers/
git commit -m "feat(frontend): add Apollo and i18n providers"
```

---

## Task 1.2.14: Create App Components

**Files:**
- Create: `frontend/src/app/App.tsx`
- Create: `frontend/src/app/Router.tsx`

**Step 1: Create App.tsx**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/app/App.tsx << 'EOF'
import { BrowserRouter } from 'react-router-dom';
import { Router } from './Router';

export function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}
EOF
```

**Step 2: Create Router.tsx**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/app/Router.tsx << 'EOF'
import { Routes, Route, Navigate } from 'react-router-dom';

export function Router() {
  return (
    <Routes>
      <Route path="/" element={<Navigate to="/dashboard" replace />} />
      <Route
        path="/dashboard"
        element={
          <div className="min-h-screen bg-gray-50 flex items-center justify-center">
            <div className="text-center">
              <h1 className="text-4xl font-bold text-gray-900 mb-4">Family Tree V2</h1>
              <p className="text-gray-600">Welcome to your family tree</p>
            </div>
          </div>
        }
      />
      <Route
        path="*"
        element={
          <div className="min-h-screen flex items-center justify-center">
            <h1 className="text-2xl text-gray-600">404 - Page Not Found</h1>
          </div>
        }
      />
    </Routes>
  );
}
EOF
```

**Step 3: Commit**

```bash
git add frontend/src/app/App.tsx frontend/src/app/Router.tsx
git commit -m "feat(frontend): add App and Router components"
```

---

## Task 1.2.15: Update Entry Point

**Files:**
- Modify: `frontend/src/main.tsx`
- Delete: `frontend/src/App.tsx` (default)

**Step 1: Delete default App.tsx**

Run:
```bash
rm -f /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/App.tsx
```

**Step 2: Update main.tsx**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/src/main.tsx << 'EOF'
import React from 'react';
import ReactDOM from 'react-dom/client';
import { ConfigProvider } from 'antd';
import { App } from '@/app/App';
import { ApolloProviderWrapper } from '@/app/providers/ApolloProvider';
import { I18nProvider } from '@/app/providers/I18nProvider';
import '@/styles/global.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider
      theme={{
        token: {
          colorPrimary: '#0ea5e9',
        },
      }}
    >
      <I18nProvider>
        <ApolloProviderWrapper>
          <App />
        </ApolloProviderWrapper>
      </I18nProvider>
    </ConfigProvider>
  </React.StrictMode>
);
EOF
```

**Step 3: Commit**

```bash
git add frontend/src/main.tsx frontend/src/App.tsx
git commit -m "feat(frontend): update entry point with all providers"
```

---

## Task 1.2.16: Update Package.json Scripts

**Files:**
- Modify: `frontend/package.json`

**Step 1: Read current package.json**

Run:
```bash
cat /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/package.json
```

**Step 2: Update scripts section**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm pkg set scripts.dev="vite"
npm pkg set scripts.build="tsc && vite build"
npm pkg set scripts.preview="vite preview"
npm pkg set scripts.test="vitest"
npm pkg set scripts.test:coverage="vitest run --coverage"
npm pkg set scripts.test:ui="vitest --ui"
npm pkg set scripts.lint="eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
npm pkg set scripts.lint:fix="eslint . --ext ts,tsx --fix"
npm pkg set scripts.format="prettier --write \"src/**/*.{ts,tsx,css}\""
npm pkg set scripts.typecheck="tsc --noEmit"
```

**Step 3: Verify scripts**

Run:
```bash
cat package.json | grep -A 12 '"scripts"'
```

Expected:
```json
"scripts": {
  "dev": "vite",
  "build": "tsc && vite build",
  "preview": "vite preview",
  "test": "vitest",
  "test:coverage": "vitest run --coverage",
  "test:ui": "vitest --ui",
  "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
  "lint:fix": "eslint . --ext ts,tsx --fix",
  "format": "prettier --write \"src/**/*.{ts,tsx,css}\"",
  "typecheck": "tsc --noEmit"
}
```

**Step 4: Commit**

```bash
git add frontend/package.json
git commit -m "chore(frontend): add npm scripts"
```

---

## Task 1.2.17: Create .gitignore

**Files:**
- Create: `frontend/.gitignore`

**Step 1: Create .gitignore**

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend/.gitignore << 'EOF'
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage
coverage

# Environment
.env
.env.local
.env.*.local
EOF
```

**Step 2: Commit**

```bash
git add frontend/.gitignore
git commit -m "chore(frontend): add .gitignore"
```

---

## Task 1.2.18: Verify Build and Lint

**Files:**
- None (verification only)

**Step 1: Run TypeScript check**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm run typecheck
```

Expected: No errors

**Step 2: Run lint**

Run:
```bash
npm run lint
```

Expected: No errors (or fixable warnings)

**Step 3: Fix any lint issues**

Run:
```bash
npm run lint:fix
npm run format
```

**Step 4: Run build**

Run:
```bash
npm run build
```

Expected: Build succeeds

---

## Task 1.2.19: Verify Dev Server

**Files:**
- None (verification only)

**Step 1: Start dev server**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
npm run dev
```

Expected output:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

**Step 2: Open browser and verify**

Open http://localhost:3000

Expected: Welcome page displays "Family Tree V2" with Ant Design styling

**Step 3: Stop server**

Press `Ctrl+C`

---

## Task 1.2.20: Final Commit

**Step 1: Verify all files exist**

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/frontend
ls -la src/
ls -la src/app/
ls -la public/locales/
```

Expected: All directories and files present

**Step 2: Final commit**

```bash
git add frontend/
git commit -m "feat(frontend): complete frontend project initialization

- Initialize Vite React TypeScript project
- Add Ant Design and Tailwind CSS
- Configure Apollo Client for GraphQL
- Setup i18n with EN/VI support
- Add Jotai for state management
- Configure ESLint, Prettier, and Vitest
- Create feature-based directory structure"
```

---

## Success Criteria Checklist

After completing all tasks, verify:

- [ ] `npm run dev` starts dev server on port 3000
- [ ] App displays welcome page without errors
- [ ] `npm run lint` passes without errors
- [ ] `npm run build` succeeds
- [ ] `npm run test` runs (may have no tests)
- [ ] Tailwind classes work (check in browser)
- [ ] Ant Design components render correctly
- [ ] i18n is configured and ready
- [ ] GraphQL client is configured

---

## Execution Handoff

Plan complete and saved to `docs/plans/2026-03-07-task-1.2-implementation.md`.

**Two execution options:**

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

**Which approach?**
