# Task 1.3: Setup Docker Infrastructure - Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Create Docker Compose configuration for MongoDB and MinIO services with initialization scripts.

**Files:**
- Create: `docker-compose.yml`
- Create: `docker-compose.dev.yml`
- Create: `scripts/mongo-init.js`

---

## Step 1: Create scripts directory

Run:
```bash
mkdir -p /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/scripts
```

Verify: `ls scripts/` shows directory exists

---

## Step 2: Create mongo-init.js

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/scripts/mongo-init.js << 'EOF'
db = db.getSiblingDB('familytree');

// Create collections
db.createCollection('users');
db.createCollection('families');
db.createCollection('members');
db.createCollection('relationships');
db.createCollection('media');
db.createCollection('sources');
db.createCollection('timelines');

// Create indexes
db.users.createIndex({ username: 1 }, { unique: true });
db.users.createIndex({ familyId: 1 });

db.families.createIndex({ ownerId: 1 });
db.families.createIndex({ 'shareLinks.token': 1 });

db.members.createIndex({ familyId: 1 });
db.members.createIndex({ familyId: 1, 'personalInfo.firstName': 1, 'personalInfo.lastName': 1 });
db.members.createIndex(
  { 'personalInfo.firstName': 'text', 'personalInfo.lastName': 'text', bio: 'text', occupation: 'text' },
  { weights: { 'personalInfo.firstName': 10, 'personalInfo.lastName': 10, bio: 5, occupation: 3 } }
);

db.relationships.createIndex({ familyId: 1 });
db.relationships.createIndex({ person1Id: 1, person2Id: 1, type: 1 }, { unique: true });

db.media.createIndex({ familyId: 1 });
db.media.createIndex({ uploadedBy: 1 });

db.sources.createIndex({ familyId: 1 });

db.timelines.createIndex({ familyId: 1 });
db.timelines.createIndex({ memberIds: 1 });

print('Database initialized successfully');
EOF
```

Verify: `cat scripts/mongo-init.js` shows file contents

---

## Step 3: Create docker-compose.yml

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/docker-compose.yml << 'EOF'
version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_ROOT_PASSWORD}
    volumes:
      - mongodb-data:/data/db
      - ./scripts/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - familytree-network

  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY}
    command: server /data --console-address ":9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - familytree-network

volumes:
  mongodb-data:
  minio-data:

networks:
  familytree-network:
    driver: bridge
EOF
```

Verify: `cat docker-compose.yml` shows file contents

---

## Step 4: Create docker-compose.dev.yml

Run:
```bash
cat > /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/docker-compose.dev.yml << 'EOF'
version: '3.8'

services:
  mongodb:
    ports:
      - "27017:27017"

  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
EOF
```

Verify: `cat docker-compose.dev.yml` shows file contents

---

## Step 5: Copy .env.example to .env

Run:
```bash
cp /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/.env.example /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2/.env
```

Verify: `cat .env` shows environment variables

---

## Step 6: Start Docker services

Run:
```bash
cd /Users/CuocThien/Desktop/NCT/WEB/FamilyTreeV2
docker-compose up -d
```

Expected output:
```
[+] Running 3/3
 ✔ Network familytreev2_familytree-network  Created
 ✔ Container familytreev2-mongodb-1         Started
 ✔ Container familytreev2-minio-1           Started
```

---

## Step 7: Verify services are healthy

Run:
```bash
docker-compose ps
```

Expected: Both services show "healthy" status

---

## Step 8: Test MongoDB connection

Run:
```bash
docker-compose exec mongodb mongosh -u admin -p ${MONGO_ROOT_PASSWORD:-password} --authenticationDatabase admin --eval "db.getSiblingDB('familytree').getCollectionNames()"
```

Expected: Array of collection names including 'users', 'families', 'members'

---

## Step 9: Test MinIO access

Run:
```bash
curl http://localhost:9000/minio/health/live
```

Expected: Returns "OK" or empty 200 response

---

## Step 10: Commit

Run:
```bash
git add docker-compose.yml docker-compose.dev.yml scripts/mongo-init.js .env
git commit -m "feat(infra): add Docker Compose configuration for MongoDB and MinIO"
```

---

## Success Criteria

- [ ] `docker-compose up -d` starts both services
- [ ] MongoDB accessible at localhost:27017
- [ ] MinIO accessible at localhost:9000
- [ ] MinIO console accessible at localhost:9001
- [ ] MongoDB indexes created automatically
- [ ] Both services show "healthy" status
