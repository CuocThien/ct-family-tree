# Task 1.2: Initialize Frontend Project - Design Document

**Date:** 2026-03-07
**Status:** Approved
**Phase:** 1 - Foundation

---

## Overview

Initialize the React frontend project with Vite, TypeScript, Ant Design, Tailwind CSS, Jotai for state management, Apollo Client for GraphQL, and React Router for navigation. Setup feature-based organization and i18n structure.

---

## Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Build tool | Vite | Fast HMR, native TypeScript |
| State management | Jotai | Atomic, minimal boilerplate |
| UI framework | Ant Design + Tailwind | Comprehensive components + utility styling |
| GraphQL client | Apollo Client | Industry standard, good caching |
| i18n | react-i18next | Standard solution, good async loading |
| Testing | Vitest + Testing Library | Fast, Vite-native |

---

## Project Structure

```
frontend/
├── public/
│   ├── favicon.ico
│   └── locales/
│       ├── en/
│       │   └── translation.json
│       └── vi/
│           └── translation.json
├── src/
│   ├── app/
│   │   ├── App.tsx
│   │   ├── Router.tsx
│   │   └── providers/
│   │       ├── ApolloProvider.tsx
│   │       ├── JotaiProvider.tsx
│   │       └── I18nProvider.tsx
│   ├── features/
│   │   └── .gitkeep
│   ├── shared/
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   └── MainLayout.tsx
│   │   │   └── ui/
│   │   │       └── .gitkeep
│   │   ├── hooks/
│   │   │   └── .gitkeep
│   │   └── utils/
│   │       └── .gitkeep
│   ├── graphql/
│   │   └── client.ts
│   ├── i18n/
│   │   └── index.ts
│   ├── styles/
│   │   └── global.css
│   ├── main.tsx
│   └── vite-env.d.ts
├── tests/
│   ├── setup.ts
│   └── .gitkeep
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.node.json
├── tailwind.config.js
├── postcss.config.js
├── .eslintrc.cjs
├── .prettierrc
├── vitest.config.ts
└── .gitignore
```

---

## Dependencies

### Production

| Package | Version | Purpose |
|---------|---------|---------|
| react | ^18.2.0 | UI library |
| react-dom | ^18.2.0 | DOM rendering |
| react-router-dom | ^6.20.0 | Routing |
| @apollo/client | ^3.8.0 | GraphQL client |
| graphql | ^16.8.0 | GraphQL core |
| jotai | ^2.5.0 | State management |
| antd | ^5.12.0 | UI components |
| react-hook-form | ^7.48.0 | Form handling |
| @hookform/resolvers | ^3.3.0 | Form validation resolvers |
| zod | ^3.22.0 | Schema validation |
| react-i18next | ^13.5.0 | React i18n bindings |
| i18next | ^23.7.0 | i18n framework |
| i18next-browser-languagedetector | ^7.2.0 | Browser language detection |
| date-fns | ^2.30.0 | Date utilities |

### Development

| Package | Version | Purpose |
|---------|---------|---------|
| @vitejs/plugin-react | ^4.2.0 | Vite React plugin |
| vite | ^5.0.0 | Build tool |
| typescript | ^5.3.0 | TypeScript compiler |
| @types/react | ^18.2.0 | React type definitions |
| @types/react-dom | ^18.2.0 | React DOM type definitions |
| tailwindcss | ^3.3.0 | CSS framework |
| postcss | ^8.4.0 | CSS processor |
| autoprefixer | ^10.4.0 | CSS vendor prefixes |
| vitest | ^1.0.0 | Unit testing framework |
| @testing-library/react | ^14.1.0 | React testing utilities |
| @testing-library/jest-dom | ^6.1.0 | Jest DOM matchers |
| jsdom | ^23.0.0 | DOM environment for tests |
| eslint | ^8.55.0 | Linting |
| eslint-plugin-react | ^7.33.0 | React ESLint rules |
| eslint-plugin-react-hooks | ^4.6.0 | Hooks ESLint rules |
| eslint-plugin-react-refresh | ^0.4.0 | React Refresh linting |
| @typescript-eslint/eslint-plugin | ^6.0.0 | TypeScript ESLint rules |
| @typescript-eslint/parser | ^6.0.0 | TypeScript ESLint parser |
| prettier | ^3.0.0 | Code formatting |

---

## Configuration Files

### vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 3000,
    proxy: {
      '/graphql': {
        target: 'http://localhost:4000',
        changeOrigin: true,
      },
    },
  },
});
```

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### tailwind.config.js

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  // Disable preflight to avoid conflicts with Ant Design
  corePlugins: {
    preflight: false,
  },
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
        },
      },
    },
  },
  plugins: [],
};
```

### postcss.config.js

```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

### vitest.config.ts

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    globals: true,
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      exclude: ['node_modules/', 'tests/'],
    },
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

### .eslintrc.cjs

```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react/jsx-runtime',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
    'react/prop-types': 'off',
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
  settings: {
    react: {
      version: 'detect',
    },
  },
};
```

### .prettierrc

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

---

## Core Files

### index.html

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Family Tree</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

### src/main.tsx

```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import { ConfigProvider } from 'antd';
import { App } from '@/app/App';
import { ApolloProviderWrapper } from '@/app/providers/ApolloProvider';
import { I18nProvider } from '@/app/providers/I18nProvider';
import '@/styles/global.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider
      theme={{
        token: {
          colorPrimary: '#0ea5e9',
        },
      }}
    >
      <I18nProvider>
        <ApolloProviderWrapper>
          <App />
        </ApolloProviderWrapper>
      </I18nProvider>
    </ConfigProvider>
  </React.StrictMode>
);
```

### src/app/App.tsx

```typescript
import { BrowserRouter } from 'react-router-dom';
import { Router } from './Router';

export function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}
```

### src/app/Router.tsx

```typescript
import { Routes, Route, Navigate } from 'react-router-dom';

export function Router() {
  return (
    <Routes>
      <Route path="/" element={<Navigate to="/dashboard" replace />} />
      <Route
        path="/dashboard"
        element={
          <div className="min-h-screen bg-gray-50 flex items-center justify-center">
            <div className="text-center">
              <h1 className="text-4xl font-bold text-gray-900 mb-4">Family Tree V2</h1>
              <p className="text-gray-600">Welcome to your family tree</p>
            </div>
          </div>
        }
      />
      <Route
        path="*"
        element={
          <div className="min-h-screen flex items-center justify-center">
            <h1 className="text-2xl text-gray-600">404 - Page Not Found</h1>
          </div>
        }
      />
    </Routes>
  );
}
```

### src/app/providers/ApolloProvider.tsx

```typescript
import { ApolloProvider as BaseApolloProvider, ApolloClient, InMemoryCache, HttpLink } from '@apollo/client';
import { ReactNode } from 'react';

const httpLink = new HttpLink({
  uri: '/graphql',
  credentials: 'include',
});

const client = new ApolloClient({
  link: httpLink,
  cache: new InMemoryCache(),
});

interface ApolloProviderWrapperProps {
  children: ReactNode;
}

export function ApolloProviderWrapper({ children }: ApolloProviderWrapperProps) {
  return <BaseApolloProvider client={client}>{children}</BaseApolloProvider>;
}
```

### src/app/providers/I18nProvider.tsx

```typescript
import { ReactNode, Suspense } from 'react';
import { I18nextProvider } from 'react-i18next';
import { Spin } from 'antd';
import i18n from '@/i18n';

interface I18nProviderProps {
  children: ReactNode;
}

export function I18nProvider({ children }: I18nProviderProps) {
  return (
    <Suspense
      fallback={
        <div className="min-h-screen flex items-center justify-center">
          <Spin size="large" />
        </div>
      }
    >
      <I18nextProvider i18n={i18n}>{children}</I18nextProvider>
    </Suspense>
  );
}
```

### src/i18n/index.ts

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

i18n.use(LanguageDetector).use(initReactI18next).init({
  fallbackLng: 'en',
  supportedLngs: ['en', 'vi'],
  interpolation: {
    escapeValue: false,
  },
  detection: {
    order: ['localStorage', 'navigator'],
    caches: ['localStorage'],
  },
  resources: {
    en: {
      translation: {
        welcome: 'Welcome to Family Tree',
        dashboard: 'Dashboard',
        members: 'Members',
        tree: 'Family Tree',
        settings: 'Settings',
      },
    },
    vi: {
      translation: {
        welcome: 'Chào mừng đến với Gia Phả',
        dashboard: 'Bảng điều khiển',
        members: 'Thành viên',
        tree: 'Cây gia phả',
        settings: 'Cài đặt',
      },
    },
  },
});

export default i18n;
```

### src/graphql/client.ts

```typescript
import { ApolloClient, InMemoryCache, HttpLink, from } from '@apollo/client';
import { setContext } from '@apollo/client/link/context';
import { onError } from '@apollo/client/link/error';

const httpLink = new HttpLink({
  uri: '/graphql',
  credentials: 'include',
});

const authLink = setContext((_, { headers }) => {
  const token = localStorage.getItem('auth_token');
  return {
    headers: {
      ...headers,
      authorization: token ? `Bearer ${token}` : '',
    },
  };
});

const errorLink = onError(({ graphQLErrors, networkError }) => {
  if (graphQLErrors) {
    graphQLErrors.forEach(({ message, locations, path }) => {
      console.error(`[GraphQL error]: Message: ${message}, Location: ${locations}, Path: ${path}`);
    });
  }
  if (networkError) {
    console.error(`[Network error]: ${networkError}`);
  }
});

export const apolloClient = new ApolloClient({
  link: from([errorLink, authLink, httpLink]),
  cache: new InMemoryCache({
    typePolicies: {
      Query: {
        fields: {
          members: {
            merge: false,
          },
        },
      },
    },
  }),
  defaultOptions: {
    watchQuery: {
      fetchPolicy: 'cache-and-network',
    },
  },
});
```

### src/styles/global.css

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Ant Design overrides */
.ant-layout {
  min-height: 100vh;
}

.ant-layout-sider {
  background: #001529;
}

/* Custom scrollbar */
.scrollbar-thin::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

.scrollbar-thin::-webkit-scrollbar-track {
  background: transparent;
}

.scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: rgba(0, 0, 0, 0.2);
  border-radius: 3px;
}

.scrollbar-thin::-webkit-scrollbar-thumb:hover {
  background-color: rgba(0, 0, 0, 0.3);
}

/* Custom utilities */
@layer utilities {
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
```

---

## Translation Files

### public/locales/en/translation.json

```json
{
  "welcome": "Welcome to Family Tree",
  "dashboard": "Dashboard",
  "members": "Members",
  "tree": "Family Tree",
  "settings": "Settings",
  "login": "Login",
  "logout": "Logout",
  "register": "Register",
  "loading": "Loading..."
}
```

### public/locales/vi/translation.json

```json
{
  "welcome": "Chào mừng đến với Gia Phả",
  "dashboard": "Bảng điều khiển",
  "members": "Thành viên",
  "tree": "Cây gia phả",
  "settings": "Cài đặt",
  "login": "Đăng nhập",
  "logout": "Đăng xuất",
  "register": "Đăng ký",
  "loading": "Đang tải..."
}
```

---

## Package.json Scripts

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --ext ts,tsx --fix",
    "format": "prettier --write \"src/**/*.{ts,tsx,css}\"",
    "typecheck": "tsc --noEmit"
  }
}
```

---

## Success Criteria

- [ ] `npm run dev` starts dev server on port 3000
- [ ] App displays welcome page without errors
- [ ] `npm run lint` passes without errors
- [ ] `npm run build` succeeds
- [ ] `npm run test` runs (even with no tests)
- [ ] Tailwind classes work alongside Ant Design
- [ ] i18n switches between EN/VI
- [ ] GraphQL client configured and ready

---

## Atomic Steps

| # | Step | Command | Verification |
|---|------|---------|--------------|
| 1 | Create frontend directory | `mkdir frontend && cd frontend` | Directory exists |
| 2 | Initialize Vite project | `npm create vite@latest . -- --template react-ts` | Vite files created |
| 3 | Install production deps | `npm install react-router-dom @apollo/client graphql jotai antd react-hook-form @hookform/resolvers zod react-i18next i18next i18next-browser-languagedetector date-fns` | Dependencies installed |
| 4 | Install dev deps | `npm install -D tailwindcss postcss autoprefixer vitest @testing-library/react @testing-library/jest-dom jsdom @types/react @types/react-dom eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-react-refresh` | DevDependencies installed |
| 5 | Create tsconfig.json | Write file | TypeScript compiles |
| 6 | Create vite.config.ts | Write file | Vite runs |
| 7 | Create tailwind.config.js | Write file | Tailwind works |
| 8 | Create postcss.config.js | Write file | PostCSS works |
| 9 | Create vitest.config.ts | Write file | Tests run |
| 10 | Create .eslintrc.cjs | Write file | Linting works |
| 11 | Create .prettierrc | Write file | Formatting works |
| 12 | Create .gitignore | Write file | Git ignores files |
| 13 | Create directory structure | `mkdir -p src/app/providers src/features src/shared/components/layout src/shared/hooks src/shared/utils src/graphql src/i18n src/styles tests public/locales/en public/locales/vi` | Directories exist |
| 14 | Create global.css | Write file | Styles work |
| 15 | Create main.tsx | Write file | App renders |
| 16 | Create App.tsx | Write file | Router works |
| 17 | Create Router.tsx | Write file | Routes work |
| 18 | Create ApolloProvider.tsx | Write file | GraphQL ready |
| 19 | Create I18nProvider.tsx | Write file | i18n ready |
| 20 | Create i18n/index.ts | Write file | Translations load |
| 21 | Create graphql/client.ts | Write file | Client configured |
| 22 | Create translation files | Write JSON files | Translations exist |
| 23 | Create tests/setup.ts | Write file | Tests setup |
| 24 | Update package.json scripts | Edit file | Scripts work |
| 25 | Run dev server | `npm run dev` | Server on :3000 |
| 26 | Verify app loads | Open browser | Welcome page shown |
| 27 | Run lint | `npm run lint` | No errors |
| 28 | Run build | `npm run build` | Build succeeds |
| 29 | Commit | `git add . && git commit` | Commit created |
